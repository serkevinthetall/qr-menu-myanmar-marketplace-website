# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.

## Colors

All UI colors live in `constants/colors.ts` — single source of truth. Use `useAppColors()` or Paper `useTheme()`. Never hardcode hex in components.

| Token | Light |
|-------|-------|
| background | `#F6F8FB` |
| primary | `#2563EB` |
| success | `#10B981` |
| text | `#0F172A` |
| card | `#FFFFFF` |
| border | `#E2E8F0` |
